# 파이참 설정 파일 무시
.idea/

# 파이썬 가상환경 및 캐시 무시
venv/
__pycache__/
*.pyc

# 리액트(프론트엔드) 라이브러리 무시 (엄청 큼)
frontend/node_modules/
frontend/build/
.DS_Store

# (선택) 모델 파일이 100MB 넘으면 에러 나므로 무시
# (포트폴리오 제출용이면 코드가 중요하지 모델 파일 자체는 제외해도 됩니다)
*.pth